syntax = "proto3";
package chat.blog;

import "gogoproto/gogo.proto";

option go_package = "chat/x/blog/types";

// Params defines the parameters for the module.
message Params {
  option (gogoproto.goproto_stringer) = false;
  
}
